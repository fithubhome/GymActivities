spring:
  datasource:
    url: jdbc:mysql://localhost:3307/gym_activities?allowPublicKeyRetrieval=true&useSSL=false
    username: service_user
    password: password
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL8Dialect
    database: mysql
    show-sql: true
